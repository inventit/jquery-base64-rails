RSpec.describe Jquery::Base64::Rails do
  it "has a version number" do
    expect(Jquery::Base64::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
